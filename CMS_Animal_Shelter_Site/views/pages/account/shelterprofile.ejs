<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>CritterCrew - Shelter Profile</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <link rel="stylesheet" href="/stylesheets/info/shelter.css">
</head>
<body>
     <!--Including navigation bar-->
     <%- include(includePath + '/template/header') %>
    <div class="container mt-5">
        <ul class="list-group list-group-flush list-group-horizontal-md">
            <!--Shelter Profile Picture-->
            <li class="list-group-item text-center border-0">
                <img src="../../images/Portrait_Placeholder.png" id="shelter-pic">
            </li>

            <!--Shelter Info-->
            <li class="list-group-item">
                <h1><%= locals.user.username %></h1>
                <table class="table">
                    <tr>
                        <th>Address:</th>
                        <td><%= locals.user.Shelter.address %></td>
                    </tr>
                    <tr>
                        <th>Animals cared for:</th>
                        <td><%= locals.user.Shelter.animals %></td>
                    </tr>
                    <tr>
                        <th> About: </th>
                        <td><%= locals.user.Shelter.about %></td>
                    </tr>
                    <tr>
                        <th>Website:</th>
                        <td><a href="http://<%= locals.user.Shelter.website %>""><%= locals.user.Shelter.website %></a></td>
                    </tr>
                </table>
            </li>
        </ul>

        <!--Buttons-->
        <div class="btn-toolbar justify-content-center">

            <% if(locals.currentuser) { %>

                <% if(locals.user.username === locals.currentuser.username) {%>
                    <a href="/messages/inbox">
                        <button type="button" class="btn btn-primary m-1">View Inbox</button>
                    </a>
                    <a href="/user/settings">
                        <button type="button" class="btn btn-secondary m-1">Account Settings</button>
                    </a>
                <% } else { %>
                    <a href=<%- `/messages/compose?recipient=${locals.user.username}` %>>
                        <button type="button" class="btn btn-primary m-1">Send Message</button>
                    </a>
                    <button type="button" class="btn btn-outline-danger m-1" onclick="reportAccount('<%= locals.user.username %>', 'User', 'Investigate user report')">Report User</button>
                    <% if(locals.currentuser && locals.currentuser.isadmin) { %>
                        <button type="button" class="btn btn-outline-danger m-1" onclick="banAccount('<%= locals.user.username %>')">Ban User</button>
                    <% } %>
                <% } %>
            <% } %>
            
        </div>
    </div>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz" crossorigin="anonymous"></script>
</body>
</html>