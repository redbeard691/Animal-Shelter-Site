<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>CritterCrew - <%= blog.title %></title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
</head>
<body>
    <%- include(includePath + '/template/header') %>

    <div class="container mt-5">
        <div class="card">
            <div class="card-header">
                <h1><%= blog.title %></h1>
                <p class="text-muted">By: <%= blog.author %> | Date Created: <%= new Date(blog.createdAt).toLocaleDateString() %></p>
            </div>
            <div class="card-body">
                <p><%- blog.content %></p>
                <% if (blog.featuredImage) { %>
                    <img src="<%= blog.featuredImage %>" class="img-fluid mt-3" alt="Featured Image">
                <% } %>
                </div>
            <div class="card-footer">
                <% if (locals.currentuser && locals.currentuser.isadmin) { %>
                    <form action="/pages/info/editblog/<%- blog.slug %>" method="get">
                        <button class="btn btn-secondary mb-2" type="submit">Edit Blog Post</button>
                    </form>
                    <form action="/pages/info/deleteblog/<%- blog.slug %>" method="post">
                        <button class="btn btn-danger mb-2" type="submit">Delete Blog Post</button>
                    </form>
                <% } %>
                <a href="/pages/info/blog" class="btn btn-primary">Back to Blog List</a>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz" crossorigin="anonymous"></script>
</body>
</html>